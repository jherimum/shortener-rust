[package]
name = "proto"
version = "0.1.0"
edition = "2024"

[dependencies]
tonic = { version = "0.13.0" }
prost = { version = "0.13.0" }


[build-dependencies]
tonic-build = { version = "0.13.0", features = ["prost"] }
